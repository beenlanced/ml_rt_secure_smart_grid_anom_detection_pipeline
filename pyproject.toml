[project]
name = "iiot-project"
version = "0.1.0"
description = "A Real-Time, Secure Smart Grid Anomaly Detection Pipeline"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.15.0",
    "numpy>=2.5.2",
    "pytest>=9.1.1",
    "ruff>=0.16.4",
]
